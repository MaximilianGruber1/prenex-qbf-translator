TestFormula("a <-> b", "a <-> b");
            TestFormula("a <-> b", "a <-> b");

            TestFormula("?a a <-> b", "?a (a <-> b)");
            TestFormula("#a a <-> b", "#a (a <-> b)");
            TestFormula("a <-> ?b b", "?b (a <-> b)");
            TestFormula("a <-> #b b", "#b (a <-> b)");
            TestFormula("?a a <-> ?b b", "?a ?b (a <-> b)");
            TestFormula("?a a <-> #b b", "?a #b (a <-> b)");
            TestFormula("#a a <-> ?b b", "#a ?b (a <-> b)");
            TestFormula("#a a <-> #b b", "#a #b (a <-> b)");